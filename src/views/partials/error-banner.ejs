<% if (typeof errors !== 'undefined' && errors.length > 0) { %>
  <div class="error-banner" role="alert">
    <% for (const err of errors) { %>
      <p><strong><%= err.repo %>:</strong> <%= err.message %></p>
    <% } %>
  </div>
<% } %>
